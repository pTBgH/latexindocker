% !TEX encoding = UTF-8
\chapter{Phương pháp phân tích và thiết kế kiến trúc Microservice}

\section{Thiết kế hướng miền}

\subsection{Giới thiệu về Thiết kế hướng miền}
Thiết kế hướng miền (Domain-Driven Design - DDD) là phương pháp luận thiết kế phần mềm được Eric Evans giới thiệu trong tác phẩmkinh điển của mình \cite{evans2003}. DDD đặt miền nghiệp vụ (business domain) làm trung tâm của quá trình phát triển, thay vì xuất phát từ các yếu tố kỹ thuật như cơ sở dữ liệu hay framework.

Triết lý cốt lõi của DDD nhấn mạnh sự cộng tác chặt chẽ giữa các chuyên gia kỹ thuật và chuyên gia nghiệp vụ để xây dựng \textbf{Ngôn ngữ chung (Ubiquitous Language)} - một ngôn ngữ thống nhất được sử dụng xuyên suốt trong mã nguồn, tài liệu thiết kế và giao tiếp giữa các bên liên quan. DDD được cấu trúc thành hai tầng thiết kế chính:
\begin{itemize}
    \item \textbf{Thiết kế Chiến lược (Strategic Design):} Tập trung vào phân tích và phân rã miền nghiệp vụ lớn thành các phần nhỏ hơn, dễ quản lý. Đây được coi là "bản đồ địa hình nghiệp vụ" của hệ thống.
    \item \textbf{Thiết kế Chiến thuật (Tactical Design):} Cung cấp các khối xây dựng (building blocks) và mẫu thiết kế cụ thể để triển khai mô hình miền trong mã nguồn.
\end{itemize}

\subsection{Tính cần thiết của DDD trong kiến trúc Microservices}
Thách thức lớn nhất khi xây dựng Microservices không nằm ở việc chọn Kubernetes hay Docker, mà nằm ở việc xác định \textbf{ranh giới dịch vụ (service boundaries)} một cách chính xác. Nếu ranh giới này được thiết kế sai, hệ thống sẽ nhanh chóng rơi vào bẫy "Distributed Monolith" (Khối nguyên khối phân tán) - một trạng thái tồi tệ nơi mọi thay đổi nhỏ về nghiệp vụ đều kéo theo sự sửa đổi đồng bộ trên nhiều dịch vụ. 

DDD giải quyết vấn đề này thông qua các cơ chế:
\begin{enumerate}
    \item \textbf{Định nghĩa ranh giới tự nhiên:} Ngữ cảnh giới hạn (Bounded Context) cung cấp phương pháp xác định ranh giới dựa trên nghiệp vụ.
    \item \textbf{Ưu tiên nguồn lực:} Phân loại miền con (Subdomain) theo nguyên tắc Pareto - Core Domain thường chỉ chiếm 20\% giá trị hệ thống nhưng đòi hỏi 80\% nỗ lực sáng tạo.
    \item \textbf{Quản lý nhất quán dữ liệu:} Hỗ trợ thiết kế hệ thống phân tán với tính nhất quán cuối cùng (eventual consistency) thông qua Domain Events.
    \item \textbf{Tuân thủ Định luật Conway:} Đảm bảo cấu trúc hệ thống phản ánh cấu trúc tổ chức, giảm ma sát trong phát triển.
\end{enumerate}

\subsection{Thiết kế Chiến lược (Strategic Design)}

\subsubsection{Phân loại Miền con (Subdomain) - La bàn đầu tư nguồn lực}
DDD đề xuất phân rã miền nghiệp vụ thành các Subdomain để định hướng chiến lược đầu tư:

\begin{table}[htbp]
\centering
\caption{Phân loại Miền con trong DDD}
\label{tab:subdomain_types}
\begin{tabularx}{\textwidth}{|l|X|c|X|}
\hline
\rowcolor[HTML]{EFEFEF} 
\textbf{Loại miền con} & \textbf{Đặc điểm} & \textbf{Lợi thế} & \textbf{Chiến lược Microservice} \\ \hline
\textbf{Core Domain} & Độc nhất, logic phức tạp, thay đổi thường xuyên. & Rất cao & Xây dựng in-house với đội ngũ giỏi nhất. \\ \hline
\textbf{Supporting} & Cần thiết để vận hành nhưng không tạo khác biệt. & Thấp & Xây dựng nội bộ hoặc outsource. \\ \hline
\textbf{Generic} & Phổ biến, đã được chuẩn hóa (Email, Auth). & Không & Chiến lược "Mua thay vì Xây" (SaaS/Open Source). \\ \hline
\end{tabularx}
\end{table}

\subsubsection{Ngữ cảnh Giới hạn (Bounded Context)}
\textbf{Định nghĩa:} Ngữ cảnh Giới hạn (Bounded Context - BC) là ranh giới rõ ràng nơi một mô hình miền cụ thể có ý nghĩa thống nhất. Một từ ngữ có thể mang nhiều ý nghĩa khác nhau tùy thuộc ngữ cảnh. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{images/boundedcontext.png}
    \caption{Từ ngữ "Sản phẩm" trong các Bounded Context khác nhau}
    \label{fig:bounded_context}
\end{figure}


Ví dụ: thực thể "Sản phẩm" (Product) trong \textit{Sales Context} tập trung vào giá và khuyến mãi, trong khi ở \textit{Marketing Context}, "Sản phẩm" lại được biểu diễn thông qua các khía cạnh như định vị thị trường, nhóm khách hàng mục tiêu, thông điệp thương hiệu và mức độ tương tác của các chiến dịch marketing, phản ánh mục tiêu thu hút và xây dựng nhận thức thương hiệu.


\textbf{Mối quan hệ với Microservice:} 
Lý tưởng nhất là tỷ lệ 1:1 (Một Microservice bao trùm trọn vẹn một Bounded Context). Tuy nhiên, một Microservice không nên nhỏ hơn một Aggregate và không nên lớn hơn một Bounded Context.

\subsubsection{Context Mapping - Định hình quan hệ xã hội và kỹ thuật}
Context Map phản ánh mối quan hệ giữa các đội nhóm và sự phụ thuộc kỹ thuật:

\begin{table}[htbp]
\centering
\caption{Các mẫu quan hệ chính trong Context Mapping}
\label{tab:context_mapping}
\begin{tabularx}{\textwidth}{|l|X|c|}
\hline
\rowcolor[HTML]{EFEFEF} 
\textbf{Mẫu quan hệ} & \textbf{Mô tả} & \textbf{Sự phụ thuộc} \\ \hline
\textbf{Partnership} & Hai đội phối hợp chặt chẽ, thất bại một bên là thất bại cả hai. & Rất cao \\ \hline
\textbf{Shared Kernel} & Chia sẻ một phần nhỏ mô hình (thư viện Value Objects chung). & Cao \\ \hline
\textbf{Customer-Supplier} & Upstream cung cấp dịch vụ, Downstream có quyền yêu cầu. & Trung bình \\ \hline
\textbf{Conformist} & Downstream buộc phải chấp nhận mô hình của Upstream. & Cao \\ \hline
\textbf{ACL} & Lớp chống tham nhũng bảo vệ mô hình miền tinh sạch. & Thấp \\ \hline
\textbf{Open Host (OHS)} & Cung cấp giao thức chuẩn công khai cho nhiều Downstream. & Thấp \\ \hline
\end{tabularx}
\end{table}

\subsection{Thiết kế Chiến thuật (Tactical Design)}

\subsubsection{Entity và Value Object}
\begin{itemize}
    \item \textbf{Entity (Thực thể):} Đối tượng được xác định bởi định danh (Identity) duy nhất, có vòng đời và trạng thái thay đổi (Ví dụ: Order, Candidate).
    \item \textbf{Value Object (Đối tượng giá trị):} Đối tượng xác định bởi giá trị thuộc tính, bất biến (Immutable) và an toàn đa luồng (Ví dụ: Address, Money).
\end{itemize}

\subsubsection{Cụm thực thể}
Cụm thực thể (Aggregate) là cụm các Entity và Value Object liên quan chặt chẽ, được coi là một đơn vị nhất quán duy nhất cho các thay đổi dữ liệu. Mọi thay đổi phải thông qua \textbf{Aggregate Root}. 
Các quy tắc thiết kế cụm thực thể trong Microservice:
\begin{enumerate}
    \item \textbf{Bảo vệ Invariants:} Đảm bảo mọi quy tắc nghiệp vụ luôn đúng sau giao dịch.
    \item \textbf{Thiết kế nhỏ (Small Aggregates):} Tránh "God Aggregate" (Cụm thực thể lớn quá mức) gây vấn đề hiệu năng và tranh chấp khóa (locking).
    \item \textbf{Tham chiếu bằng ID:} Các cụm thực thể không chứa tham chiếu trực tiếp đến nhau, chỉ giữ ID để tạo ranh giới vật lý cứng.
    \item \textbf{Tính nhất quán cuối cùng:} Cập nhật các cụm thực thể liên quan bất đồng bộ qua Domain Events.
\end{enumerate}

\subsubsection{Dịch vụ miền và dịch vụ ứng dụng - Domain Service vs Application Service}
Domain Service vs Application Service}
DDD giúp tránh \textit{Anemic Domain Model} (Mô hình miền thiếu máu - thực thể chỉ có dữ liệu, không có hành vi) bằng cách phân định:
\begin{itemize}
    \item \textbf{Domain Service:} Chứa logic nghiệp vụ không thuộc về một Entity cụ thể, nằm trong lớp Core.
    \item \textbf{Application Service:} Điều phối luồng công việc (Facade), không chứa logic nghiệp vụ, nằm ở lớp vỏ ứng dụng.
\end{itemize}

\subsection{Kiến trúc hệ thống - Vỏ bọc bảo vệ Domain}
\subsubsection{Hexagonal Architecture (Ports and Adapters)}
Đây là kiến trúc trung tâm cho DDD Microservices, đảm bảo logic nghiệp vụ không phụ thuộc kỹ thuật:
\begin{itemize}
    \item \textbf{Core (Hexagon):} Chứa Domain Entities, Value Objects và Domain Services.
    \item \textbf{Ports:} Các Interface định nghĩa cách giao tiếp (Inbound/Outbound).
    \item \textbf{Adapters:} Triển khai cụ thể (REST Controller, Kafka Consumer, SQL Repository).
\end{itemize}

\subsection{Giao tiếp và Tính nhất quán dữ liệu phân tán}
\subsubsection{Transactional Outbox Pattern - Giải quyết "Dual-Write"}
Khi một dịch vụ vừa phải ghi Database vừa phải gửi sự kiện lên Kafka, nguy cơ mất đồng bộ là rất cao. Giải pháp Outbox thực hiện:
\begin{enumerate}
    \item Ghi Event vào bảng \textit{Outbox} trong cùng một Transaction với dữ liệu nghiệp vụ.
    \item Sử dụng \textbf{Log-based CDC} (Change Data Capture) như Debezium để đọc Transaction Log và đẩy lên Message Broker.
\end{enumerate}

\subsection{Case Study: Phân tích Online Boutique với DDD}
Hệ thống demo của Google Cloud gồm 11 microservices minh họa việc áp dụng DDD:
\begin{itemize}
    \item \textbf{Core Domain (40\%):} \textit{Product Catalog}, \textit{Cart}, \textit{Checkout}. Đây là các dịch vụ tạo lợi thế cạnh tranh.
    \item \textbf{Supporting Domain (40\%):} \textit{Frontend}, \textit{Payment}, \textit{Shipping}.
    \item \textbf{Generic Domain (20\%):} \textit{Email Service}, \textit{Currency Conversion}.
\end{itemize}
Mối quan hệ giữa các dịch vụ được điều phối qua \textit{Saga Orchestration} (tại Checkout Service) và đồng bộ qua Kafka, đảm bảo tính nhất quán cuối cùng cho hệ thống.

\subsection{Kết luận}
Thiết kế hướng miền cung cấp khung tư duy hệ thống để xây dựng kiến trúc Microservices bền vững. Việc tôn trọng ranh giới vật lý, bảo vệ tính nhất quán thông qua Aggregate và giao tiếp bất đồng bộ qua Domain Events là chìa khóa để vận hành hệ thống phân tán hiệu quả.


% !TEX encoding = UTF-8
\section{Các mẫu thiết kế giao tiếp và tích hợp}
Trong kiến trúc Microservice, việc giao tiếp giữa các dịch vụ không còn là lời gọi hàm trong cùng bộ nhớ (in-process calls) mà trở thành các lời gọi qua mạng (network calls). Điều này đòi hỏi các mẫu thiết kế phải đảm bảo tính tin cậy, hiệu năng và khả năng mở rộng.

\subsection{Giao tiếp đồng bộ (Synchronous Communication)}
Giao tiếp đồng bộ yêu cầu dịch vụ gửi (Client) phải chờ đợi phản hồi từ dịch vụ nhận (Server) trước khi tiếp tục thực hiện các tiến trình tiếp theo.

\paragraph{REST API (Representational State Transfer):}
Đây là giao thức phổ biến nhất dựa trên HTTP. REST sử dụng các phương thức chuẩn (GET, POST, PUT, DELETE) và định dạng JSON để trao đổi dữ liệu.
\begin{itemize}
    \item \textbf{Ưu điểm:} Đơn giản, dễ kiểm thử, được hỗ trợ bởi mọi ngôn ngữ lập trình và tương thích tốt với các hệ thống bên ngoài.
    \item \textbf{Nhược điểm:} Tạo ra sự phụ thuộc về thời gian (Temporal Coupling). Nếu chuỗi gọi dịch vụ (Service Chain) quá dài, độ trễ sẽ tích tụ và nếu một dịch vụ ở giữa bị lỗi, toàn bộ yêu cầu sẽ thất bại.
\end{itemize}

\subsection{Giao tiếp bất đồng bộ và Kiến trúc hướng sự kiện}
Để giải quyết vấn đề phụ thuộc chặt của giao tiếp đồng bộ, giao tiếp bất đồng bộ cho phép dịch vụ gửi thông điệp và tiếp tục công việc của mình mà không cần chờ phản hồi ngay lập tức.

\subsubsection{Cơ chế Message Queuing}
Sử dụng một Message Broker (như Kafka hoặc RabbitMQ) làm trung gian. Dịch vụ gửi (Producer) đẩy thông điệp vào hàng đợi (Queue), dịch vụ nhận (Consumer) sẽ lấy thông điệp ra xử lý khi có tài nguyên rảnh. Cơ chế này giúp "là phẳng" các đỉnh tải (load leveling) và đảm bảo thông điệp không bị mất nếu dịch vụ nhận tạm thời ngoại tuyến.

\subsubsection{Kiến trúc hướng sự kiện (Event-Driven Architecture - EDA)}
Trong mô hình này, các dịch vụ giao tiếp thông qua việc phát và nhận các sự kiện (Events). 
\begin{itemize}
    \item \textbf{Domain Event:} Đại diện cho một sự kiện nghiệp vụ đã xảy ra (ví dụ: \textit{JobPublished}, \textit{CandidateApplied}).
    \item \textbf{Loose Coupling:} Dịch vụ phát sự kiện không cần biết ai sẽ nhận sự kiện đó. Điều này cho phép dễ dàng thêm các dịch vụ mới (như dịch vụ gửi thông báo, dịch vụ thống kê) vào hệ thống mà không cần sửa đổi mã nguồn của dịch vụ gốc.
\end{itemize}

\subsection{Thiết kế API Gateway và Backend for Frontend (BFF)}
Khi hệ thống có hàng chục Microservice, việc để Client (Web/Mobile) gọi trực tiếp đến từng dịch vụ là một sai lầm về bảo mật và hiệu năng.

\subsubsection{API Gateway}
API Gateway đóng vai trò là cửa ngõ duy nhất (Single Entry Point) cho tất cả các yêu cầu từ bên ngoài. Các trách nhiệm chính bao gồm:
\begin{itemize}
    \item \textbf{Routing:} Điều hướng yêu cầu đến đúng dịch vụ đích.
    \item \textbf{Authentication \& Authorization:} Kiểm tra quyền truy cập tập trung (thường tích hợp với Keycloak).
    \item \textbf{Rate Limiting:} Giới hạn số lượng yêu cầu để chống tấn công DoS.
    \item \textbf{Offloading:} Xử lý các tác vụ chung như nén dữ liệu, SSL Termination.
\end{itemize}

\subsubsection{Backend for Frontend (BFF)}
Thay vì sử dụng một API Gateway chung cho mọi thiết bị, mẫu BFF đề xuất xây dựng các API trung gian riêng biệt cho từng loại Client (Web App, Mobile iOS, Mobile Android). 
\begin{itemize}
    \item \textbf{Lý do:} Mobile Client thường có băng thông thấp và màn hình nhỏ, cần ít dữ liệu hơn Web Client. BFF giúp tổng hợp dữ liệu (Data Aggregation) từ nhiều dịch vụ thành một phản hồi duy nhất, giảm số lượng request từ thiết bị cầm tay.
\end{itemize}

\subsubsection{Nguyên tắc thiết kế và Phiên bản hóa API}
Để đảm bảo tính ổn định khi hệ thống tiến hóa, thiết kế API cần tuân thủ:
\begin{itemize}
    \item \textbf{Tính tương thích ngược:} Tránh thực hiện các thay đổi làm hỏng ứng dụng đang chạy của Client (Breaking changes).
    \item \textbf{Phiên bản hóa (Versioning):} Sử dụng phiên bản trong URL (ví dụ: \texttt{/api/v1/...}) hoặc trong Header. Điều này cho phép chạy song song nhiều phiên bản API, giúp Client có thời gian chuyển đổi dần sang phiên bản mới \cite[tr. 147]{newman2021}.
\end{itemize}

\begin{table}[htbp]
\centering
\caption{So sánh giao tiếp Đồng bộ và Bất đồng bộ}
\label{tab:sync_async_comparison}
\begin{tabularx}{\textwidth}{|l|X|X|}
\hline
\rowcolor[HTML]{EFEFEF} 
\textbf{Tiêu chí} & \textbf{Đồng bộ (REST)} & \textbf{Bất đồng bộ (Events)} \\ \hline
\textbf{Độ trễ} & Thấp (trong điều kiện bình thường). & Cao hơn (do độ trễ của Broker). \\ \hline
\textbf{Tính sẵn sàng} & Phụ thuộc vào dịch vụ nhận. & Cao, dịch vụ gửi vẫn chạy nếu dịch vụ nhận lỗi. \\ \hline
\textbf{Độ phức tạp} & Thấp, dễ phát triển và debug. & Cao, khó theo dõi luồng dữ liệu (tracing). \\ \hline
\textbf{Sự phụ thuộc} & Phụ thuộc chặt về thời gian. & Liên kết lỏng (Loosely coupled). \\ \hline
\end{tabularx}
\end{table}

\section{Quản lý dữ liệu và tính nhất quán}
Trong Microservice, mỗi dịch vụ sở hữu một cơ sở dữ liệu riêng (\textit{Database per Service}). Điều này ngăn cản việc sử dụng các truy vấn \texttt{JOIN} giữa các bảng của các dịch vụ khác nhau và các giao dịch ACID đa dịch vụ.

\subsection{Tính nhất quán cuối cùng}
Hệ thống Microservice ưu tiên tính sẵn sàng (Availability) và khả năng chịu lỗi (Partition Tolerance) theo định lý CAP. Thay vì đảm bảo dữ liệu cập nhật ngay lập tức ở mọi nơi, hệ thống đảm bảo rằng sau một khoảng thời gian, tất cả các dịch vụ sẽ đạt được trạng thái thống nhất thông qua việc truyền thông điệp.

Các mẫu thiết kế dưới đây được tổng hợp chi tiết trên Microservices.io \cite{microservices_io}.

\subsection{Mẫu CQRS (Command Query Responsibility Segregation)}
CQRS tách biệt hoàn toàn đường dẫn xử lý lệnh Ghi (Command) và lệnh Đọc (Query):
\begin{itemize}
    \item \textbf{Command Side:} Tối ưu hóa cho việc kiểm tra quy tắc nghiệp vụ và ghi dữ liệu (thường là RDBMS).
    \item \textbf{Query Side:} Tối ưu hóa cho việc tìm kiếm và hiển thị. Dữ liệu ở đây thường được "phẳng hóa" (Denormalized) và lưu trong các công cụ như Elasticsearch để đạt tốc độ truy vấn cực nhanh.
\end{itemize}

\subsection{Mẫu Saga (Quản lý giao dịch phân tán)}
Saga là một chuỗi các giao dịch cục bộ. Mỗi giao dịch cập nhật cơ sở dữ liệu bên trong một dịch vụ và phát ra một sự kiện để kích hoạt bước tiếp theo. Nếu một bước thất bại, Saga sẽ kích hoạt các \textit{Compensating Transactions} (Giao dịch bù) để hoàn tác các bước trước đó, đảm bảo tính toàn vẹn của dữ liệu nghiệp vụ \cite{newman2021}.

\subsection{Mẫu Event Sourcing}
Thay vì chỉ lưu trạng thái hiện tại (ví dụ: \textit{Số dư = 100\$}), Event Sourcing lưu trữ toàn bộ lịch sử các sự kiện đã dẫn đến trạng thái đó (\textit{Nạp 50\$, Nạp 70\$, Rút 20\$}). Điều này cung cấp khả năng kiểm toán (Audit Trail) tuyệt vời và cho phép tái tạo trạng thái hệ thống tại bất kỳ thời điểm nào trong quá khứ.





% !TEX encoding = UTF-8
\section{Hạ tầng và Vận hành}
Mặc dù Microservices mang lại nhiều lợi ích về mặt phát triển và mở rộng, nhưng trên thực tế kiến trúc này cũng làm tăng đáng kể độ phức tạp trong vận hành hệ thống. Để một hệ thống gồm nhiều dịch vụ nhỏ hoạt động trơn tru, cần có một hạ tầng tự động hóa cao và khả năng giám sát toàn diện \cite[tr. 79]{nadareishvili2016}.

\subsection{Cơ chế Service Discovery (Khám phá dịch vụ)}
Trong môi trường container hóa (như Docker hay Kubernetes), các instance của dịch vụ thường có tính chất tạm thời (ephemeral) – chúng có thể được khởi tạo, dừng lại hoặc thay đổi địa chỉ IP bất cứ lúc nào. Do đó, việc cấu hình cứng địa chỉ IP là không khả thi.

\paragraph{Cơ chế hoạt động:}
Service Discovery sử dụng một kho lưu trữ trung tâm gọi là \textit{Service Registry} (ví dụ: Consul, Eureka hoặc K8s DNS). 
\begin{itemize}
    \item \textbf{Registration:} Khi một dịch vụ khởi động, nó tự đăng ký thông tin (tên dịch vụ, địa chỉ IP, cổng) với Registry.
    \item \textbf{Discovery:} Khi dịch vụ A muốn gọi dịch vụ B, nó sẽ gửi truy vấn đến Registry để lấy danh sách các IP đang hoạt động của dịch vụ B.
    \item \textbf{Health Check:} Registry định kỳ kiểm tra tình trạng của các dịch vụ. Nếu một instance bị lỗi, Registry sẽ gỡ tên nó khỏi danh sách để tránh gửi yêu cầu đến một "dịch vụ chết".
\end{itemize}

\subsection{Cơ chế Load Balancing (Cân bằng tải)}
Load Balancing đảm bảo lưu lượng truy cập được phân phối đều cho các instance của dịch vụ, ngăn chặn tình trạng một instance bị quá tải trong khi các instance khác rảnh rỗi.

\begin{itemize}
    \item \textbf{Server-side Load Balancing:} Một bộ cân bằng tải tập trung (như Nginx, HAProxy) nhận yêu cầu và điều phối đến các dịch vụ bên dưới.
    \item \textbf{Client-side Load Balancing:} Dịch vụ gửi yêu cầu sẽ tự quyết định instance nào sẽ nhận yêu cầu dựa trên danh sách lấy từ Service Registry. Điều này giúp giảm tải cho bộ cân bằng tải tập trung và loại bỏ điểm chết duy nhất (Single Point of Failure).
\end{itemize}

\subsection{Giám sát tập trung và Observability (Khả năng quan sát)}
Với hàng chục dịch vụ chạy riêng lẻ, việc đăng nhập vào từng server để kiểm tra log là không thể. Hệ thống Microservice hiện đại yêu cầu ba trụ cột của Observability \cite{newman2021}:

\subsubsection{Centralized Logging (Log tập trung)}
Toàn bộ nhật ký hoạt động (Logs) từ tất cả các Microservice, API Gateway và Database được thu thập về một kho lưu trữ tập trung (thường sử dụng ELK Stack: Elasticsearch - Logstash - Kibana). Điều này cho phép quản trị viên tìm kiếm và phân tích lỗi trên toàn hệ thống từ một giao diện duy nhất.

\subsubsection{Distributed Tracing (Theo vết phân tán)}
Khi một yêu cầu từ người dùng đi qua chuỗi nhiều dịch vụ, việc xác định dịch vụ nào gây ra lỗi hoặc chậm trễ là rất khó khăn. 
\begin{itemize}
    \item \textbf{Correlation ID:} Mỗi yêu cầu được gán một mã định danh duy nhất ngay tại API Gateway. Mã này được truyền đi xuyên suốt qua các lời gọi liên dịch vụ.
    \item \textbf{Công cụ:} Các hệ thống như Jaeger hoặc Zipkin giúp trực quan hóa vết đi của yêu cầu dưới dạng biểu đồ thời gian (Timeline), giúp nhanh chóng phát hiện các nút thắt cổ chai (bottlenecks).
\end{itemize}

\subsubsection{Metrics và Health Monitoring}
Theo dõi các thông số định lượng như: tỷ lệ lỗi (Error Rate), thời gian phản hồi (Latency), và mức độ sử dụng tài nguyên (CPU, RAM). Các công cụ như Prometheus kết hợp với Grafana giúp cảnh báo chủ động (Proactive Alerting) trước khi sự cố nghiêm trọng xảy ra.

\subsection{Quy trình triển khai và văn hóa DevOps}
Microservice yêu cầu một quy trình triển khai liên tục (CI/CD) tự động hóa hoàn toàn. Mỗi thay đổi mã nguồn phải đi qua các bước kiểm thử tự động (Unit Test, Integration Test) trước khi được đóng gói thành Image và triển khai lên môi trường sản xuất mà không cần sự can thiệp thủ công \cite[tr. 257]{newman2021}.

\paragraph{Thiết kế cho sự thất bại (Design for Failure):}
Trong hệ thống phân tán, lỗi là điều không thể tránh khỏi. Các kỹ sư phải thiết kế hệ thống sao cho khi một thành phần thất bại, toàn bộ ứng dụng không bị sụp đổ (Graceful Degradation). Ví dụ: Nếu dịch vụ gợi ý việc làm bị lỗi, hệ thống vẫn phải cho phép người dùng xem tin tuyển dụng và ứng tuyển bình thường.

\section{Kết luận chương 2}
Chương 2 đã trình bày chi tiết về phương pháp luận và các mẫu thiết kế cốt lõi của kiến trúc Microservice. Từ việc phân rã dịch vụ dựa trên Domain-Driven Design, thiết lập cơ chế giao tiếp hướng sự kiện đến việc xây dựng hạ tầng vận hành tự động. Đây là nền tảng lý thuyết quan trọng để tiến hành xây dựng và triển khai thực nghiệm hệ thống Tuyển dụng trực tuyến trong chương tiếp theo.




